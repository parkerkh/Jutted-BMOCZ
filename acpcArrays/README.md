# ACPC Arrays

This folder contains arrays needed for the (31,16) and (127,106) affine cyclically permutable code (ACPC) implementations. 

**Note:** you can load the arrays using the $\texttt{loadACPCarrays()}$ function in the "/functions" folder of this repository.